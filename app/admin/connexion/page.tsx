import { AdminLogin } from "../../components/AdminLogin";

export const metadata = {
  title: "Connexion administration — L’Horloger",
  robots: { index: false, follow: false },
};

export default function AdminLoginPage() {
  return <AdminLogin />;
}
